const numbers = [];
for (let i = 1; i <= 1000; i++) {
  numbers.push(i);
}
for (let i in numbers) {
    if (numbers [i] % 3=== 0) {
        numbers[a] =-1
        console.log(-1)
    } else {
        console.log(i)
    }
}
